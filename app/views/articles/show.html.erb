<article class="article-detail ">
    <h1 class="article-detail-title"><%= @article.title %></h1>

    <div class="glyphicon glyhicon-calender" id="article-date">
        <%= @article.created_at.strftime("%b %d %Y") %>
    </div>

    <div class="article-show-body">
        <%= @article.body%>
    </div>
</article>